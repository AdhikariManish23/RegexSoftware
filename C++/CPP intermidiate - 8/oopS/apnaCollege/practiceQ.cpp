#include<bits/stdc++.h>
using namespace std;
// Create a User class and properties : id(private) , username(public) & password(private). Its id should be initialised in a parameterised constructor. It should have a getter and setterr for password

class User{
    private:
    int id;
    string pass;

    public:
    string username;

    User(int id){
        this->id = id;
    }

    

}

    int main(){
    
        User us1
     return 0;
}